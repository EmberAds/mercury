require './lib/mercury'

describe Mercury::Configure
  it "should set up a response to a given message"
  it "should load a configuration from a hash"
  it "should load a configuration from a YAML file"
end